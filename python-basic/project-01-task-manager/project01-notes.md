
`project01-notes.md` 写你这次踩过的坑：

```markdown
# Project 01 Notes

## Fixed problems
- choice should be string, not int
- read_tasks should use "r", not "a"
- CSV fields must match dict keys
- planned_hours and actual_hours need float()
- search result should be judged after the loop
- top task should compare with current top task